import React from "react";

export default function Header() {
  return (
    <header>
      <nav className="navbar">
        <a href="#">Blog</a>
        <button>Sign Up</button>
      </nav>
    </header>
  );
}
